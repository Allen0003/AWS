

graph LR
    User((使用者)) -->|1. 文字| Frontend[前端 Web App]
    Frontend -->|2. HTTP APIGW[Amazon API Gateway]
    APIGW -->|3. 觸發| Lambda[AWS Lambda: Orchestrator]

    subgraph "AWS Intelligence"
        Lambda <-->|4. STT| Transcribe[Amazon Transcribe]
        Lambda <-->|5. 翻譯| Translate[Amazon Translate]
        Lambda <-->|6. NLU/對話| Lex[Amazon Lex]
        Lambda <-->|7. TTS| Polly[Amazon Polly]
    end

    Lambda -->|8. 回傳音訊串流| Frontend


元件層級		使用服務					職責
前端	S3 		html					處理 Web Audio API 錄音，透過 Presigned URL 音檔。
接入層		API Gateway				提供 RESTful API 介面，處理前後端溝通。
邏輯層		AWS Lambda (Python)		大腦： 串接各服務 API，處理資料傳遞與流程控制 (Orchestration)
語音服務		Transcribe / Polly		語音與文字的互轉。
智能服務		Lex						管理對話情境 (Intent) 與槽位 (Slot) 填寫。
翻譯服務		Translate				即時處理跨語系文字轉換。