
aws
apss1943@gmail.com
1943Apss@


iPhone

passwords -> open passwords -> all -> aws


region: us-east-1

account id: 629691902365


重新使用

# 1. 讓 aws-builder 重新開機
docker start aws-builder

# 2. 進入 到這個容器的 bash 畫面
docker exec -it aws-builder /bin/bash







TODO: 寫自己的量化交易 API：
你懂金融、懂系統，英文又好（IELTS 7.0 可以看懂所有國外券商與加密貨幣交易所的 API 文件）。你可以自己架個 Docker，寫寫 Python/Go，去跑你自己的自動化交易、套利程式，或者當個造市商（Market Maker）賺點皮毛。


TODO: kiro multi agents


TODO: 做側專案（Side Project）接案：
海外很多小型的系統整合、智慧合約（Smart Contract）建置，在平台上（如 Upwork）常常是幾千美金在結算的。



TODO: 將工作內容「產品化」（Micro-SaaS 或模板販售）

如果你上班剛好會寫某些腳本、自動化工具，或是常常整理繁瑣的表格：

做法： 把它包裝成 Notion 模板、Excel 財務模型、或是自動化小腳本，上架到 Gumroad 或 Notion Market。

為什麼輕鬆： 這是典型的「被動收入」。花幾小時做好一次，之後丟在網路上，有陌生人買就是純賺，不需要跟客戶開會或交期壓力。




TODO: 微型任務 / 資料標註（Micro-tasks & AI Training）

如果你希望是那種「不用動腦、點一點就有錢」的打發時間型工作：

做法： 透過 Remotasks、Outlier、DataAnnotation.tech 等平台參與 AI 模型訓練。

為什麼輕鬆： 工作內容通常是幫 AI 的回答評分、挑出錯字、或者進行簡單的邏輯校對。只要會打字、看得懂英文，隨時隨地用手機或零碎時間就能做，按小時或按件計酬。





TODO:

1. 跨區即時聊天室 (WebSockets API Gateway + Lambda + DynamoDB + ElastiCache Redis)

    怎麼玩：

        建置一個支援雙向通訊的 WebSockets 聊天室（類似 Slack 或 Discord 的簡易版）。

        透過 API Gateway WebSocket 協議 接收連線，用 Lambda 處理訊息廣播。

        使用 DynamoDB 儲存歷史訊息與連線中的 User Session，並用 ElastiCache (Redis) 來管理即時連線的 Pub/Sub 頻道與快取，確保多個 Lambda 執行個體間能同步廣播。

    技術亮點：學會 WebSocket 在 Serverless 架構下的持久連線管理與 Redis 的 Pub/Sub 應用。




TODO:
4. 具備 JWT 驗證與資安防護的完整 RESTful API (API Gateway + Lambda + Cognito + WAF)

    怎麼玩：

        開發一組完整的後端 API（例如部落格或電商的 CRUD 系統）。

        整合 Amazon Cognito 實作使用者的註冊、登入與 JWT (JSON Web Token) 身分驗證與授權。

        在 API Gateway 前方掛上 AWS WAF (Web Application Firewall)，設定 IP 黑白名單、Rate Limiting（防阻 DDoS 或暴力破解攻擊）。

    技術亮點：不只寫程式，還要懂身分識別（IAM / Cognito）與企業級的 API 資安防禦。