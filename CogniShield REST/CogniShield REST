

具備 JWT 驗證與資安防護的完整 RESTful API (API Gateway + Lambda + Cognito + WAF)

    開發一組完整的後端 API（例如部落格或電商的 CRUD 系統）。

    整合 Amazon Cognito 實作使用者的註冊、登入與 JWT (JSON Web Token) 身分驗證與授權。

    在 API Gateway 前方掛上 AWS WAF (Web Application Firewall)，設定 IP 黑白名單、Rate Limiting（防阻 DDoS 或暴力破解攻擊）。

    技術亮點：不只寫程式，還要懂身分識別（IAM / Cognito）與企業級的 API 資安防禦。



第一步：建立資料庫與基礎 CRUD Lambda（後端核心） (K)

先確保你的後端程式碼能運作，並具備基本的資料存取能力。

    在 DynamoDB 建立一張資料表（例如 Posts 或 Products，設定主鍵）。

    撰寫 AWS Lambda 函式（建議用 Python 或 Node.js），實作 CRUD（建立、讀取、更新、刪除）的邏輯。

    設定 Lambda 的 IAM 角色，賦予其讀寫 DynamoDB 的權限。



第二步：建立 Amazon Cognito 使用者池（身分驗證）

實作使用者的註冊、登入，並核發 JWT。

    在 Amazon Cognito 建立一個 User Pool（使用者池）。

    設定密碼政策、允許的登入屬性（例如 Email）。

    建立一個 App Client（用於前後端對接）。

    測試註冊一個新帳號、登入，並取得 ID Token / Access Token (JWT)。


pool ID: us-east-1_Aio7dISnt

client id: 4egojc3n5175bds8obmk0n902p

Invoke URL:
https://v4m3faa2z7.execute-api.us-east-1.amazonaws.com/dev



第三步：建立 Amazon API Gateway 並串接 Lambda

打造對外的 RESTful API 入口。

    建立一個 HTTP API 或 REST API（建議 REST API 以便後續掛載 WAF 和進階授權）。

    將 API 的各個路由（例如 /posts）對應到第一步寫好的 Lambda 函式。

    部署 API 並透過 Postman 測試基本的 CRUD 是否通暢。




第四步：在 API Gateway 整合 Cognito JWT 驗證

讓 API 具備身分驗證能力，未帶有效 JWT 的請求會被直接拒絕。

    在 API Gateway 設定 Cognito Authorizer（授權方），將第二步的 Cognito User Pool 綁定上去。

    將需要保護的 API 路由（例如 POST, PUT, DELETE）的授權設定改為這個 Authorizer。

    再次使用 Postman 測試：沒帶 JWT 應該拿到 401 Unauthorized；帶上有效的 JWT 才能成功操作。


curl -X GET https://v4m3faa2z7.execute-api.us-east-1.amazonaws.com/dev/posts \
-H "Authorization: 貼上你的_ID_Token_字串"


Cognito 使用者池（User Pool）帳號！
apss1943@gmail.com
1943Apss@





OAuth 2.0 授權流程 (Authorization Code Grant)
用 Cognito 強迫有token 的才可以進來

token 產生 -> Amazon Cognito  -> 進到 user pool -> 然後找 view login page

然後用 登入

apss1943@gmail.com
1943Apss@

然後把裡面找到的 secret + 登入後的網址 貼給 ai 叫他產生 get or post 的指令









第五步：部署 AWS WAF 並掛載到 API Gateway（資安防護）

要收錢 開完就先關起來


阻擋惡意攻擊、限制流量與 DDoS。

建立 Web ACL：

    前往 AWS WAF & Shield 主控台。

    重要：切換到對應的 Region（地區，例如 us-east-1），因為你的 API Gateway 是 Regional 的。

    點擊 Create web ACL，名稱輸入 MyApiWaf，Resource type 選擇 Regional resources。

掛載 API Gateway：

    在 Associated AWS resources 點擊 Add aws resources，選擇 API Gateway，並選中你的 API。

設定 Rate Limiting（流量限制）：

    在 Rules 頁面點擊 Add rules -> Add my own rules and rule groups。

    選擇 Rate-based rule，設定例如：單一 IP 在 2 分鐘內最多發送 10 次請求。

    超過限制的動作（Action）選擇 Block，按下儲存。

完成建立：一路點下一步直到完成。這樣你的 API 就多了一層 WAF 防護網。






http://my-secure-blog-frontend-2026.s3-website-us-east-1.amazonaws.com


第六步：用 S3 建立靜態前端並結合 Amplify 拿 Token

目標：擺脫純命令列，用 S3 託管前端網頁，並用 Amplify 實現一鍵登入。

    建立 S3 靜態網站：

        前往 S3 服務，點擊 Create bucket，建立一個用來放前端網頁的 Bucket（例如 my-frontend-app-bucket）。

        取消勾選「Block all public access」（允許公開讀取，或透過 CloudFront 結合，此處以最簡單的 S3 靜態網站託管為例）。

        在 Bucket 屬性（Properties）最下方開啟 Static website hosting，設定首頁為 index.html。

    寫一個極簡的 index.html：

        在裡面引入 AWS Amplify SDK。

        寫一個簡單的登入按鈕，點擊時觸發 Amplify 的 signInWithRedirect()，自動跳轉到 Cognito。

    設定 Cognito 回呼網址：

        把你 S3 的靜態網站網址（或 CloudFront 網址）加回 Cognito App Client 的 Allowed callback URLs 中。




第七步：全端整合測試與作品集 README 撰寫

目標：驗證整體防護網，並完美收官放上 GitHub。

    模擬攻擊測試（驗證 WAF）：

        用寫好的腳本或連續狂按重新整理超過你在 WAF 設定的頻率（例如 5 分鐘 100 次）。

        確認 API Gateway 開始回傳 403 Forbidden，代表 WAF 成功發揮阻擋效果！

    整理架構圖（Architecture Diagram）：

        用 Draw.io 或 Excalidraw 畫一張架構圖：S3 (前端) ➡️ Cognito (登入) ➡️ API Gateway + WAF (受保護的 API) ➡️ Lambda ➡️ DynamoDB。

    撰寫漂亮的 README.md：

        說明專案架構、使用的 AWS 服務（Cognito, API Gateway, WAF, S3, Lambda, DynamoDB）。

        列出技術亮點（如：OAuth 2.0 授權、API Key/Token 防護、WAF Rate Limiting 禦敵）。

        把程式碼推上 GitHub（記得絕對不要把 Client Secret 放進去），完成你的頂級個人雲端側專案！

